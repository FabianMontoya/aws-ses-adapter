{
  "LABEL": {
    "name": "help wanted",
    "color": "008000"
  },
  "CHECKS": {
    "regexp": "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\\([^)]+\\))?!?: .{1,88}$",
    "regexpFlags": "",
    "ignoreLabels": ["dependabot", "dependencies"]
  },
  "MESSAGES": {
    "success": "PR title follows the conventional commits convention",
    "failure": "PR title must follow the Conventional Commits format: <type>[optional scope][!]: <description> (e.g. feat(client): add retry support)",
    "notice": ""
  }
}
